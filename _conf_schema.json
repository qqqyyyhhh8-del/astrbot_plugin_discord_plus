{
  "typing_enabled": {
    "description": "是否在 AstrBot 等待 LLM 回复时向 Discord 显示“正在输入”状态。",
    "type": "bool",
    "default": true
  },
  "mention_fix_enabled": {
    "description": "是否把 AstrBot 默认的 At 组件转换成 Discord 可识别的提及格式。",
    "type": "bool",
    "default": true
  },
  "reply_reference_enabled": {
    "description": "是否在 Discord 中回复用户时自动引用原消息。",
    "type": "bool",
    "default": true
  }
}
